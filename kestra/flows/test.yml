id: test
namespace: sds.infra

tasks:
  - id: submit_to_spark
    type: io.kestra.plugin.scripts.shell.Commands
    taskRunner: 
      type: io.kestra.plugin.core.runner.Process
    commands:
      # On installe le client docker si l'image ne l'a pas
      - docker ps  > "dockerps.txt" # Si cette commande répond, on a gagné.
    outputFiles:
      - "dockerps.txt"

